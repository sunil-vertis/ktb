/**
 * Tab row data is consumed inside {@link ./product-details-page-block}.
 * If this block is placed alone on a page, it renders nothing (no surrounding PDP chrome).
 */
export default function ProductDetailsAnchorTabsCmsBlock() {
  return null
}
